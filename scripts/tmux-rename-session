#!/usr/bin/env bash
set -euo pipefail
target="${1:?Usage: tmux-rename-session <session>}"
printf 'Renaming "%s"\nNew name: ' "$target"
read -r name
if [ -n "$name" ]; then
  tmux rename-session -t "$target" "$name"
fi
clear
